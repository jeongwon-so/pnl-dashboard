# CLAUDE.md

이 프로젝트에서 매번 말하지 않아도 항상 지켜야 할 규칙입니다.

## 규칙

1. **파일 경로를 코드에 쓰지 않는다.** 사용자가 그때그때 업로드한 파일을 사용한다. (하드코딩된 경로 금지)
2. **엑셀·CSV 파일은 앱 폴더 밖에 둔다.** 그리고 `.gitignore`로 제외한다.
3. **API 키는 코드에 넣지 않는다.** `.env`에서 읽어 온다.
4. **답변은 항상 한국어로 한다.**
